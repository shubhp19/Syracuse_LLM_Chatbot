# Web scraping
requests
beautifulsoup4

# Embeddings — ONNX-based, no PyTorch, Python 3.14 compatible
fastembed==0.8.0

# Vector database
chromadb==0.5.5

# AI — Groq API
groq

# Chat UI — no version pin, gets pillow with Python 3.14 wheels
streamlit

# Env vars
python-dotenv
